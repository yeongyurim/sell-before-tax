# 실수령 MVP 구현 브리프

> 이 문서는 Claude Code에게 전달하는 구현 지시서입니다. 아래 명세대로 작동하는 웹 프로토타입을 만듭니다.

---

## 0. 한 줄 요약

사용자가 보유한 해외주식과 필요한 현금 금액을 입력하면, **양도소득세를 가장 적게 내면서 그 금액을 마련하는 매도 조합**을 계산해 보여주는 단일 페이지 웹서비스.

---

## 1. 기술 스택 (고정)

| 항목 | 선택 |
|---|---|
| 프레임워크 | Next.js 14+ (App Router) |
| 언어 | TypeScript (strict) |
| 스타일 | Tailwind CSS |
| 상태 관리 | React 내장 (useState/useReducer). 외부 라이브러리 금지 |
| 차트 | Recharts |
| 배포 | Vercel |
| 테스트 | Vitest |

**중요 제약: 모든 세금 계산과 최적화는 클라이언트(브라우저)에서 실행한다.** 보유 종목·매입단가는 민감 금융정보이므로 서버로 전송하지 않는다. 백엔드 API는 만들지 않는다.

---

## 2. 도메인 규칙 (한국 해외주식 양도소득세)

이 절의 규칙은 **정확히** 구현해야 한다. 계산 로직의 근거다.

### 2.1 과세 구조

- **과세 대상**: 해외 상장 주식·ETF의 양도차익
- **과세 기간**: 매년 1월 1일 ~ 12월 31일 (**결제일 기준**, 체결일 아님)
- **기본공제**: 연 250만원 (`BASIC_DEDUCTION = 2_500_000`)
- **세율**: 양도소득세 20% + 지방소득세 2% = **22%** (`TAX_RATE = 0.22`)
- **손익통산**: 같은 과세기간 내 해외주식 간 이익과 손실을 합산한다
- **이월결손금 공제 없음**: 올해 순손실이 나도 내년으로 이월되지 않는다
- **신고**: 다음 해 5월 확정신고

### 2.2 세액 계산식

```
realizedTotal = priorRealizedGain + Σ (x_i × perShareGain_i)
taxableBase   = max(0, realizedTotal - 2_500_000)
tax           = taxableBase × 0.22
```

- `priorRealizedGain`: 올해 이미 확정된 실현손익 (음수 가능)
- `x_i`: 종목 i의 매도 수량 (정수)
- `perShareGain_i`: 종목 i의 주당 원화 실현손익

### 2.3 환율 처리 (주의)

원화 기준으로 과세하므로 **환차익도 과세 대상에 포함**된다. 매수 시점 환율과 매도 시점 환율을 각각 적용한다.

```
perShareGain_i = (currentPrice_i × fxSell) - (avgBuyPrice_i × fxBuy_i)
```

**MVP 구현 방침**: 입력 부담을 줄이기 위해 기본값은 `fxBuy_i = fxSell = 현재환율`(단일 환율)로 두되, 종목별 `fxBuy_i`를 선택 입력할 수 있게 한다. 단일 환율 모드일 때는 화면에 다음 안내를 표시한다.

> 매수 시점 환율을 입력하지 않으면 환차손익이 반영되지 않아 실제 세액과 차이가 날 수 있습니다.

### 2.4 MVP에서 제외하는 것 (화면에 한계로 명시)

- 대주주 요건 (지분율·시가총액 기준)
- 해외 배당소득 및 금융소득종합과세
- 매매 수수료·제비용 (`필요경비`) — 향후 반영
- 국내주식, 국내상장 ETF (과세 체계가 완전히 다름)

---

## 3. 데이터 모델

```typescript
/** 사용자가 보유한 해외주식 1종목 */
export interface Holding {
  id: string;
  ticker: string;          // 예: "AAPL"
  name: string;            // 예: "애플"
  quantity: number;        // 보유 수량 (정수, > 0)
  avgBuyPrice: number;     // 평균 매입단가 (외화, USD)
  currentPrice: number;    // 현재가 (외화, USD)
  fxBuy?: number;          // 매수 시점 환율 (미입력 시 fxSell 사용)
  locked: boolean;         // true면 매도 대상에서 제외
}

/** 최적화 입력 */
export interface OptimizeInput {
  holdings: Holding[];
  priorRealizedGain: number;   // 올해 이미 실현한 손익 (원화, 음수 가능)
  targetCash: number;          // 필요 현금 (원화, 세후 기준)
  fxSell: number;              // 매도 시점 환율 (원/USD)
  portfolioWeight: number;     // 0~1. 클수록 포트폴리오 비중 유지를 중시
}

/** 매도 조합 1개 */
export interface SellPlan {
  label: string;                          // "세금 최소" | "균형" | "비중 유지"
  lots: { holdingId: string; sellQty: number }[];
  grossProceeds: number;   // 매도 대금 합계 (원화, 세전)
  realizedGain: number;    // 이번 매도로 발생한 실현손익
  totalRealized: number;   // priorRealizedGain 포함 연간 실현손익
  taxableBase: number;
  tax: number;
  netCash: number;         // grossProceeds - tax  (실수령액)
  portfolioDrift: number;  // 0~1, 낮을수록 기존 비중 유지
  meetsTarget: boolean;
}

export interface OptimizeResult {
  plans: SellPlan[];       // 최대 3개
  baseline: SellPlan;      // 비교 기준: 수익률 높은 순 단순 매도
  savings: number;         // baseline.tax - plans[0].tax
}
```

---

## 4. 핵심 알고리즘

### 4.1 문제 정의

결정변수 `x_i ∈ {0, 1, ..., q_i}` (종목 i의 매도 수량, `locked === true`인 종목은 `x_i = 0` 고정)

**제약**

```
netCash = Σ(x_i × currentPrice_i × fxSell) - tax  ≥  targetCash
```

**목적함수** (가중합 최소화)

```
cost = tax + portfolioWeight × DRIFT_SCALE × portfolioDrift
```

`DRIFT_SCALE`은 drift(0~1)를 원화 단위로 환산하는 상수. `targetCash × 0.1` 정도로 두고 튜닝한다.

**portfolioDrift 정의**: 매도 전후 비중 벡터의 L1 거리 절반

```
w_i  = (q_i × price_i) / Σ(q_j × price_j)              // 매도 전 비중
w'_i = ((q_i - x_i) × price_i) / Σ((q_j - x_j) × price_j)  // 매도 후 비중
drift = 0.5 × Σ |w_i - w'_i|
```

전량 청산 시 분모가 0이 되므로 이 경우 `drift = 1`로 처리한다.

### 4.2 왜 단순 정렬로 안 되는가 (구현 시 유의점)

기본공제 250만원 때문에 세금 함수가 **구간 선형(piecewise linear)**이다. 실현손익이 250만원 이하 구간에서는 한계세율이 0이고, 초과하는 순간 22%가 된다. 따라서 "수익률 낮은 순 정렬"이나 "손실 종목 우선"같은 단일 기준 그리디는 최적해를 놓친다. 이익 종목과 손실 종목을 **함께** 조합해 실현손익 합계를 공제 한도 근처에 착지시키는 것이 핵심이다.

### 4.3 해법 (2단계)

**1단계 — 초기해 생성 (결정론적, 3종)**

| 이름 | 전략 |
|---|---|
| `greedyLossFirst` | 평가손실 종목을 전량 매도 → 부족분을 주당이익 낮은 순으로 채움 |
| `greedyDeductionFit` | 실현손익 합계가 `2,500,000 - priorRealizedGain`에 최대한 근접하도록 이익 종목을 부분 매도, 부족한 현금은 손실 종목으로 보충 |
| `greedyProRata` | 모든 종목을 현재 비중대로 균등 비율 매도 (drift 최소) |

**2단계 — 국소탐색 개선**

각 초기해에 대해 hill climbing을 수행한다.

- 이웃 생성: (a) 한 종목의 `x_i`를 ±1, ±5, ±10, ±(q_i의 5%) 만큼 변경 (b) 두 종목 간 매도 금액을 유지하며 수량 교환
- 제약을 위반하는 이웃은 버린다
- `cost`가 개선되면 이동, 개선이 없으면 종료
- **시간 예산 200ms** 또는 최대 5,000회 반복 중 먼저 도달하는 조건에서 중단
- 랜덤 재시작 3회

**3단계 — 결과 선별**

`portfolioWeight`를 0.0 / 0.5 / 1.0 세 값으로 각각 최적화를 돌려 `plans` 3개를 만든다. 라벨은 각각 "세금 최소", "균형", "비중 유지".

**baseline**: 수익률(수익률 = `currentPrice / avgBuyPrice - 1`) 높은 순으로 목표 현금을 채울 때까지 매도. 대부분의 투자자가 실제로 하는 방식이며, 절감액 비교의 기준이 된다.

### 4.4 구현 위치

```
lib/tax.ts        — 세액 계산 순수 함수 (calcTax, calcPerShareGain, evaluatePlan)
lib/optimizer.ts  — 초기해 생성 + 국소탐색 (optimize)
lib/baseline.ts   — 단순 매도 시뮬레이션
```

`lib/tax.ts`의 함수는 전부 순수 함수로 작성하고 부수효과를 두지 않는다. 테스트 대상이다.

---

## 5. 화면 명세

단일 페이지, 3개 섹션을 세로로 배치한다.

### 5.1 섹션 A — 입력

**보유 종목 테이블** (행 추가/삭제 가능)

| 티커 | 종목명 | 수량 | 평균단가($) | 현재가($) | 매도 제외 |
|---|---|---|---|---|---|

- 하단에 "행 추가" 버튼
- **CSV 붙여넣기** 지원: textarea에 `티커,수량,평균단가,현재가` 형식을 붙여넣으면 테이블에 파싱해 채운다
- **예시 데이터 채우기** 버튼 필수 (심사위원이 즉시 체험할 수 있어야 한다)

**보조 입력**

- 올해 이미 실현한 손익 (원화, 기본값 0)
- 필요 금액 (원화)
- 현재 환율 (원/USD, 기본값 1,380)

### 5.2 섹션 B — 결과

**상단 요약 카드**

```
┌─────────────────────────────────────┐
│  절감 가능액                          │
│  2,340,000원                         │
│  단순 매도 2,490,000원 → 최적 150,000원 │
└─────────────────────────────────────┘
```

**조합 카드 3개** (가로 배치, 클릭 시 상세 펼침)

각 카드에 표시할 것: 라벨, 세금, 실수령액, 매도 종목 수, 포트폴리오 유지율(`1 - drift`)

**비교 차트** (Recharts 막대그래프)

단순 매도 vs 3개 조합의 세금을 나란히 비교

### 5.3 섹션 C — 상세 내역

선택한 조합의 종목별 매도 수량과 세금 계산 과정을 단계별로 표시한다.

```
매도 대금 합계        52,300,000원
이번 매도 실현손익     +2,180,000원
올해 기존 실현손익     +180,000원
─────────────────────────────
연간 실현손익 합계     +2,360,000원
기본공제              -2,500,000원
과세표준              0원
세금 (22%)            0원
─────────────────────────────
실수령액              52,300,000원
```

### 5.4 디자인 방향

- 금융 서비스에 맞는 절제된 톤. 흰 배경, 회색 계열 텍스트, 강조는 단일 포인트 컬러 하나만
- 숫자는 반드시 천 단위 구분 쉼표. 원화는 `원`, 외화는 `$` 접두
- 절감액은 크고 명확하게. 이 서비스의 가치 제안이다
- 모바일 대응 필수 (테이블은 가로 스크롤 허용)
- 다크모드는 구현하지 않는다 (시간 절약)

---

## 6. 테스트 (필수)

`lib/tax.test.ts`에 최소 다음 케이스를 작성한다. **전부 통과해야 한다.**

| # | priorRealizedGain | 이번 매도 실현손익 | 기대 세금 | 검증 포인트 |
|---|---|---|---|---|
| 1 | 0 | 2,000,000 | 0 | 공제 한도 이내 |
| 2 | 0 | 2,500,000 | 0 | 공제 한도 경계 |
| 3 | 0 | 5,000,000 | 550,000 | (500만-250만)×22% |
| 4 | 1,800,000 | 1,200,000 | 110,000 | 기존 실현손익 합산 |
| 5 | 0 | -3,000,000 | 0 | 순손실 시 세금 0 |
| 6 | 5,000,000 | -3,000,000 | 0 | 손익통산으로 과세표준 0 |
| 7 | -1,000,000 | 5,000,000 | 330,000 | 음수 기존손익 통산 |

추가로 `lib/optimizer.test.ts`에 다음을 검증한다.

- 최적해의 `netCash`가 항상 `targetCash` 이상이다
- `locked: true`인 종목의 `sellQty`가 항상 0이다
- 모든 `sellQty`가 정수이며 `0 ≤ sellQty ≤ quantity`를 만족한다
- 이익 종목과 손실 종목이 섞인 시나리오에서 최적해의 세금이 baseline보다 작거나 같다

---

## 7. 파일 구조

```
app/
  layout.tsx
  page.tsx                 // 단일 페이지 (섹션 A/B/C 조합)
  globals.css
components/
  HoldingsTable.tsx        // 섹션 A 종목 입력
  CsvPasteBox.tsx
  InputPanel.tsx           // 실현손익/목표금액/환율
  ResultSummary.tsx        // 절감액 요약 카드
  PlanCards.tsx            // 조합 카드 3개
  TaxBreakdown.tsx         // 섹션 C 계산 내역
  ComparisonChart.tsx      // Recharts
lib/
  types.ts
  tax.ts
  optimizer.ts
  baseline.ts
  sampleData.ts            // 예시 데이터
  format.ts                // 숫자 포맷 유틸
  tax.test.ts
  optimizer.test.ts
```

---

## 8. 예시 데이터 (`sampleData.ts`)

환율 1,380원 기준. 이익 종목과 손실 종목이 섞여 있어 최적화 효과가 드러나는 구성이다.

| 티커 | 종목명 | 수량 | 평균단가 | 현재가 |
|---|---|---|---|---|
| AAPL | 애플 | 120 | 150.00 | 232.00 |
| MSFT | 마이크로소프트 | 60 | 280.00 | 425.00 |
| NVDA | 엔비디아 | 40 | 62.00 | 178.00 |
| TSLA | 테슬라 | 80 | 265.00 | 218.00 |
| GOOGL | 알파벳 | 50 | 175.00 | 196.00 |
| PLTR | 팔란티어 | 200 | 41.00 | 28.50 |
| AMD | AMD | 90 | 158.00 | 121.00 |
| SCHD | 슈드 ETF | 300 | 27.50 | 29.10 |

기본 시나리오: `priorRealizedGain = 1,800,000`, `targetCash = 50,000,000`

---

## 9. 구현 순서

시간이 제한적이므로 아래 순서로 진행하고, 각 단계가 끝나면 동작을 확인한다.

1. `lib/types.ts`, `lib/tax.ts` 작성 → `tax.test.ts` 통과 확인
2. `lib/baseline.ts` 작성 (단순 매도 시뮬레이션)
3. `lib/optimizer.ts` — 초기해 3종만 먼저 구현하고 동작 확인
4. 최소 UI로 결과가 나오는 것 확인 (스타일 없이)
5. `optimizer.ts`에 국소탐색 추가 → `optimizer.test.ts` 통과 확인
6. UI 정리, 차트 추가, 반응형 대응
7. 한계 고지 문구, 예시 데이터 버튼 추가
8. Vercel 배포

**5번 이후는 시간이 부족하면 잘라낸다.** 초기해 3종만으로도 baseline 대비 유의미한 절감이 나오므로 데모는 성립한다.

---

## 10. 화면에 반드시 표시할 고지 문구

푸터에 다음을 명시한다.

> 본 서비스는 세무 상담이 아닌 계산 보조 도구입니다. 확정된 세법에 따른 시뮬레이션 결과이며, 대주주 요건·해외 배당소득·매매 수수료 등 개별 사정에 따라 실제 세액과 차이가 있을 수 있습니다. 특정 종목의 투자 가치를 판단하거나 매수를 추천하지 않습니다. 입력하신 보유 종목 정보는 서버로 전송되지 않으며 브라우저에서만 처리됩니다.

이 문구는 규제 관점에서 필수다. 서비스가 투자일임업·유사투자자문업에 해당하지 않음을 드러내는 장치이며, 심사에서 실제 서비스화 가능성을 평가받는 근거가 된다.

---

## 11. 하지 말 것

- 백엔드 API 라우트 생성 (계산은 전부 클라이언트)
- 외부 시세 API 연동 (수동 입력으로 충분하다)
- 로그인·회원가입
- 데이터베이스
- LLM 호출 (MVP 범위 밖. 시간이 남으면 그때 논의한다)
- 주가 예측이나 종목 추천 성격의 기능
